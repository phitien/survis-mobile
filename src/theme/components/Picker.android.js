export default (variables) => {
  const pickerTheme = {
    '.note': {
      color: '#8F8E95'
    },
    width: 90,
    marginRight: -4,
    backgroundColor: 'transparent'
  }

  return pickerTheme
}
