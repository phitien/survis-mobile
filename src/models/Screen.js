import {BASEURL} from '../constants'

export const acts = {
}
export const Screen = {
  id: '',
  params: {},
}
export default {
  loading: false, error: false, persistent: false, loadmore: false,
  acts,
  Screen,
}
